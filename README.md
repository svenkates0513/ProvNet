# ProvNet_Demographics
This code set display the API response, creates the connection with SQL Server, execute the store procs, insert or update the table in DB as per the response data and log all the process in flat file for reference.
